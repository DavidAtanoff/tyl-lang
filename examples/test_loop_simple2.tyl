// Simple loop test

fn main:
    println("Starting loop...")
    
    mut count = 0
    while count < 5:
        println("Count: ", count)
        sleep(500)
        count = count + 1
    
    println("Done!")

main()
