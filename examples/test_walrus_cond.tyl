// Debug walrus operator in condition

fn test_walrus:
    items = [1, 2, 3, 4, 5]
    println("items created")
    if (n := len(items)) > 0:
        println("Length: ", n)

fn main:
    test_walrus()

main()
