// Simplest possible unswitch test
fn test(flag: int) -> int {
    mut x := 0
    for i in range(3) {
        if flag > 0 {
            x = x + 1
        } else {
            x = x + 2
        }
    }
    return x
}

fn main() -> int {
    r := test(1)
    print("Result: {r}")
    return 0
}
