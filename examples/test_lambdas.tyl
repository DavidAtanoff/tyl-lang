// Test Lambda Expressions
println("=== Lambdas Test ===")

// Simple lambda
double = |x| => x * 2
println("Created double lambda")

// Lambda with multiple params
add = |a, b| => a + b
println("Created add lambda")

// Test calling lambdas (if supported)
// result = double(5)
// println("double(5) = {result}")

println("Lambdas Test PASSED")
