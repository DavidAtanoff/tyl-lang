// Test str() with atomic load

fn main:
    let counter: Atomic[int] = Atomic[int](0)
    counter.store(42)
    let val: int = counter.load()
    let s: str = str(val)
    println(s)
    return 0

main()
