// Minimal test for generic float issue

fn max_val[T] a: T, b: T -> T:
    if a > b:
        return a
    return b

fn main:
    // This should print 3.14 (or similar)
    m = max_val(3.14, 2.71)
    println(m)
    return 0
