// Test inline assembly

fn main:
    println("Testing inline assembly...")
    
    // Test inline assembly
    unsafe:
        asm! { "mov rax, 10", "add rax, 32" }
    
    println("Inline assembly test complete!")
    return 0
