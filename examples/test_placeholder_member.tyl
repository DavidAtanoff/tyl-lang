// Test placeholder _.field

record Point:
    x: int
    y: int

fn main():
    let p = Point(x: 42, y: 10)
    print("p.x = " + str(p.x))
    
    // Test _.field as lambda (member access)
    let getX = _.x
    let val = getX(p)
    print("getX(p) = " + str(val))
    
    print("Done!")
