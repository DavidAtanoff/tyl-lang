// Debug - disable register allocation by using more variables

fn test_many_vars:
    mut a = 1
    mut b = 2
    mut c = 3
    mut d = 4
    mut e = 5
    mut f = 6
    mut g = 7
    mut h = 8
    println("a={a} b={b} c={c} d={d} e={e} f={f} g={g} h={h}")

fn main:
    test_many_vars()

main()
