// Test 9: Memory Management
// Testing: new, delete, unsafe blocks

println("=== Memory Management Test ===")

// Heap allocation with new
ptr = new int
println("Allocated memory with new")

// Delete to free memory
delete ptr
println("Freed memory with delete")

// Unsafe block
unsafe:
    ptr2 = new int
    println("Allocated in unsafe block")
    delete ptr2
    println("Freed in unsafe block")

println("Test 9 PASSED")
