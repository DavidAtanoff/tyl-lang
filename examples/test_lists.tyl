// Test Lists/Arrays
println("=== Lists Test ===")

// List literal
numbers = [1, 2, 3, 4, 5]
println("Created list: numbers")

// List with mixed types (if supported)
mixed = [1, 2, 3]
println("Created mixed list")

// Empty list
empty = []
println("Created empty list")

// List with computed values
a = 10
b = 20
computed = [a, b, a + b]
println("Created computed list")

// Access by index (if supported)
// first = numbers[0]
// println("First element: {first}")

println("Lists Test PASSED")
