// Test 10: Records and Anonymous Structs
// Testing: record literals, field access

println("=== Records Test ===")

// Anonymous record (inline struct)
point = { x: 10, y: 20 }
println("Created point record")

person = { name: "Alice", age: 30 }
println("Created person record")

// Record with computed values
a = 5
b = 10
computed = { sum: a + b, product: a * b }
println("Created computed record")

println("Test 10 PASSED")
