// Test map with more entries and lookups

// Create a map with several entries
data = {"name": 42, "age": 25, "score": 100, "level": 5}

// Access all values
println("name: ", data["name"])
println("age: ", data["age"])
println("score: ", data["score"])
println("level: ", data["level"])

// Test non-existent key (should return 0)
println("missing: ", data["missing"])

// Update existing
data["score"] = 150
println("updated score: ", data["score"])

// Test with string values (stored as pointers)
// For now, maps store integers - string values would need additional work

println("Map tests passed!")
