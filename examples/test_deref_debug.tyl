// Debug pointer dereference issue

fn main:
    unsafe:
        mut x = 42
        println("x before: {x}")
        p = &x
        println("p (address): {p}")
        val = *p
        println("*p (dereferenced): {val}")
        println("x after: {x}")

main()
