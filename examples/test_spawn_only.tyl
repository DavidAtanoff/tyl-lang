// Test spawn only - no await
fn ret_one:
    return 1

println("Before spawn...")
h = spawn ret_one()
println("After spawn, handle obtained")
sleep(100)
println("After sleep")
println("Done - not awaiting")
