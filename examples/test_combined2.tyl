// Test combined features with walrus

fn test1:
    println("test1")
    a = b = c = 42
    println("a=", a)

fn test2:
    println("test2")
    x = 10
    if x > 5 then
        println("x > 5")
    end

fn test3:
    println("test3")
    items = [1, 2, 3]
    if (n := len(items)) > 0:
        println("len=", n)

fn main:
    test1()
    test2()
    test3()

main()
