// Test Lambda Expressions with Calls
println("=== Lambda Call Test ===")

// Simple lambda that doubles a number
double = |x| => x * 2

// Call the lambda
result = double(5)
println("double(5) = {result}")

// Lambda with multiple params
add = |a, b| => a + b
sum = add(3, 4)
println("add(3, 4) = {sum}")

// Lambda with expression
square = |n| => n * n
sq = square(6)
println("square(6) = {sq}")

println("Lambda Call Test PASSED")
