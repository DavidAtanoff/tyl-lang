// Channel with print before send

fn test_channel:
    let ch = chan[int, 1]
    print("x")
    ch <- 100
    let value = <- ch
    print("Received value: ")
    print(value)
    print("\n")

fn main -> int:
    test_channel()
    return 0
