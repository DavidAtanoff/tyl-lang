// Test String Operations
println("=== Strings Test ===")

// String literals
hello = "Hello"
world = "World"
println("hello = {hello}")
println("world = {world}")

// String interpolation
greeting = "Hello, {world}!"
println("greeting = {greeting}")

// String with numbers
num = 42
msg = "The answer is {num}"
println("msg = {msg}")

// Built-in string functions
test_str = "hello"
println("len(test_str) = {len(test_str)}")
println("upper(test_str) = {upper(test_str)}")

sub_str = "ell"
println("contains(test_str, sub_str) = {contains(test_str, sub_str)}")
println("str(42) = {str(42)}")

// Ternary with strings
result = true ? "yes" : "no"
println("true ? yes : no = {result}")

println("Strings Test PASSED")
