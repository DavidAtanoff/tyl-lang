// Debug GC allocation - with string concat
println "=== Test ==="

println "Initial: " + str(gc_stats()) + " bytes"

mut x = 10
let list1 = [x, x+1, x+2, x+3, x+4]
println "After list1: " + str(gc_stats()) + " bytes"

println "Done"
